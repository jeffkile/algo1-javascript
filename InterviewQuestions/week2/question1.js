var fifoStack = [];
var stackB = [];

var push = function (pushA) {
  fifoStack.push(pushMe); // Adds them to the end of the stack
};

var pop2B = function () {
  for(var i=0; i<fifoStack.length; i++) {
    stackB[i] = fifoStack[i];
  }
};



